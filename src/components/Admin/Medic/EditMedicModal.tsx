import type { MedicFormData } from "@/types/index"

type PropsEditMedicModal = {
    data?: MedicFormData
}

export default function EditMedicModal({ data } : PropsEditMedicModal) {

    console.log(data);
  return (
    <>

        
    </>
  )
}
